# TmuxGo 0802 Goal

## 总目标

把当前已经可用的终端工作台推进到可长期部署、可安全维护、在高输出和多主机场景下仍然稳定顺滑的状态。

核心顺序：先补齐认证和凭据安全，再处理性能与可靠性，最后扩展插件和协作能力。

## P0 认证安全中心

### 目标

让默认账号认证具备完整的日常管理能力，避免用户只能登录和退出，无法管理设备或修改凭据。

### 工作内容

- 增加安全设置页面。
- 展示当前登录设备、User-Agent、IP、创建时间、最近使用时间和过期时间。
- 支持撤销单个设备 session。
- 支持退出全部其他设备。
- 支持修改密码。
- 首次使用 `admin/admin123` 时强制修改密码。
- 检测仍使用默认密码的状态并持续提示。
- 修改密码、撤销设备和登录失败写入审计日志。
- 保持现有多设备独立 session 和自动续期行为不变。

### 验收标准

- 每台设备首次登录一次，后续刷新、断线和 Gateway 重启不要求重复输入密码。
- 当前设备登出不会影响其他设备。
- 修改密码后所有旧 access token、refresh token 和 WebSocket ticket 均失效。
- 默认密码不再被允许长期使用。
- 浏览器、Agent、插件 iframe、图片和下载请求均能正常通过认证。

## P0 远程主机凭据管理

### 目标

降低远程 SSH 凭据泄露风险，完善多主机连接方式。

### 工作内容

- 支持 SSH 私钥路径。
- 支持 SSH Agent。
- 支持 Jump Host / ProxyJump。
- 增加 `known_hosts` 校验策略。
- 对已有 `hosts.json` 做凭据格式迁移。
- 密码和私钥等敏感信息单独存储，文件权限固定为 `0600`。
- API 和前端只返回是否配置凭据，不返回敏感值。
- 连接失败时区分网络错误、主机密钥错误和认证错误。

### 验收标准

- 未配置密码时可以使用 SSH key 或 SSH Agent 连接。
- 主机配置文件和日志中不出现密码、私钥内容或 refresh token。
- 旧版主机配置可以平滑迁移。
- 主机测试结果能明确说明失败原因。

## P1 性能与流畅性

### 目标

降低高输出终端、大目录和移动端布局变化对主线程的影响。

### 工作内容

- 清理 React Query 与 Zustand 对 hosts、sessions、windows、panes 的重复状态同步。
- 将整包 Zustand 订阅改为精确 selector。
- 将终端 output 从全局 CustomEvent 热路径改为定向订阅。
- 拆分 `TerminalPane` 的终端核心、剪贴板、移动端输入、拖拽和选区逻辑。
- 统一 resize、fit 和 layout-change 调度，使用 `requestAnimationFrame` 合并高频变化。
- 拖拽面板过程中只更新局部状态，结束后再持久化。
- 根据桌面前台、移动端和后台页面调整 Gateway output flush 策略。
- 文件树增加目录缓存、搜索取消和大目录降级策略。

### 验收标准

- 连续高输出时终端输入回显保持稳定，无明显卡顿。
- resize 拖拽不会触发无意义的全局重渲染和重复 fit。
- 大目录展开不会明显阻塞终端输入。
- 桌面端、移动端、后台恢复、复制粘贴和拖拽上传回归测试通过。

## P1 统一任务中心

### 目标

统一管理上传、下载、Git、SSH 和重启等可能持续较长时间的操作。

### 工作内容

- 增加后台任务列表和任务详情。
- 展示进度、速度、耗时、日志和最终结果。
- 支持取消、重试和失败后继续。
- 支持移动端任务完成和失败通知。
- 统一任务错误码和用户可读错误信息。
- 保留重启任务的现有状态和日志能力。

### 验收标准

- 大文件上传、下载和 Git 操作不会阻塞主界面。
- 任务失败后可以看到具体原因并进行重试。
- 页面刷新后仍能看到未完成或最近完成的任务状态。

## P1 Agent 与多主机可靠性

### 目标

让 Agent、SSH 主机和本地主机的状态可观察、可恢复、可诊断。

### 工作内容

- 增加 Agent 心跳、版本和最后在线时间。
- 展示 Agent 断线原因和重连状态。
- 展示主机延迟、依赖检查和最近一次连接错误。
- 支持主机分组、标签、收藏和排序。
- 明确展示当前主机使用 SSH 还是 Agent。
- 为 Agent 注册和重连增加幂等处理。

### 验收标准

- Agent 断线后界面能在合理时间内反映状态。
- Agent 重启或 Gateway 重启后能够自动恢复连接。
- 用户可以快速判断主机不可用的具体原因。
- 多主机切换不会串用 session、文件和 Git 状态。

## P2 插件权限与版本管理

### 目标

在已有插件 iframe、Action、Storage 和 GitHub 安装能力上建立可信的插件生命周期。

### 工作内容

- 插件 manifest 增加权限声明。
- 首次使用 Action、文件或主机能力时要求授权。
- 记录插件来源、版本和 resolved commit。
- 支持更新、回滚和卸载清理。
- 安装前校验来源和 commit，避免动态分支漂移。
- 插件日志中记录权限调用和失败原因。

### 验收标准

- 插件不能默认获得未声明的主机、文件或命令能力。
- 用户可以查看、撤销和重新授权插件权限。
- 插件升级失败可以回滚到上一版本。
- 卸载插件后其资源和可选数据清理行为明确可控。

## P2 临时共享与只读访问

### 目标

支持受控的终端协作和远程排障，不扩大默认账号权限边界。

### 工作内容

- 创建指定 host/session 范围的临时访问链接。
- 支持只读终端权限。
- 支持过期时间和主动撤销。
- 默认禁止文件、Git、主机配置和插件管理能力。
- 审计共享链接的创建、使用和撤销。

### 验收标准

- 共享访问不能读取未授权的 session 或主机。
- 共享链接过期后立即失效。
- 只读访问无法发送终端输入。
- 主账号修改密码或撤销权限后共享链接同步失效。

## 质量门槛

- Gateway 单元测试、认证集成测试和 WebSocket 测试持续通过。
- 前端先建立当前全量测试失败基线，逐步修复现有终端布局、React Query mock 和异步 act 失败。
- 增加浏览器端登录、刷新、登出、多设备和资源请求 E2E 测试。
- 增加高输出终端、大目录、移动端键盘和多主机切换性能基线。
- 每项性能优化必须保留优化前后可比较的指标。

## 推荐实施顺序

1. 安全中心、首次改密和认证 E2E。
2. SSH key / Agent / known_hosts 与凭据迁移。
3. 修复前端测试基线，收缩全局状态和终端 output 热路径。
4. Agent 心跳、多主机诊断和统一任务中心。
5. 插件权限、版本回滚和临时只读共享。
