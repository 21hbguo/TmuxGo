# 任务包 A3：R4+R5 E2E 冒烟真实性 + test session 约束 + CI 基准

来源：docs/followup-review-20260923-024431.md（R4、R5）。派发自 devin@%655，回执回 %655。

## 开工准备（必须）

```bash
cd /home/guo/project/other/TmuxGo
git worktree add ../TmuxGo-wt-e2e-gate -b fix/e2e-smoke-ci master
cd ../TmuxGo-wt-e2e-gate && pnpm install --frozen-lockfile  # 如 node_modules 不可用再执行
```

只在 `TmuxGo-wt-e2e-gate` 内改动。先用 todo_write 拆任务。

## R4 · PR 冒烟要证明「真正执行」+ test session 约束

**证据**：
- `e2e/pr-smoke.spec.ts:13` 输入的命令本身含 marker，L20 用 `includes(marker)` 判成功——命令行回显即可满足断言，证明不了 shell 执行。
- 自定义事件注入输入绕过了真实 textarea 焦点/键盘路径。
- `scripts/run-e2e.ts:121` 仍建 `tmuxgo-e2e`，PR 冒烟建 `pr-smoke`；server 虽隔离但未满足「真实行为测试只用 `test` session」的项目约束（AGENTS.md）。

**要求**：
1. 所有真实 tmux 行为测试只在隔离 server 的 `test` session 内组织，window/pane 区分场景并串行；检查全部 E2E 入口（`scripts/run-e2e.ts`、`run-agent-e2e.ts`、`run-auth-e2e.ts`、`run-ssh-e2e.ts`、e2e/*.spec.ts 里建 session 的地方），不是只改一个名字。清理只针对测试专用 server，禁止动默认 socket 上的用户 session。
2. 键盘输入走真实终端输入框（textarea focus + key 事件），断言等待输出中出现「运行时计算得到」的独立结果行——marker 由测试内计算（如 `echo $((6*7))` 期望 `42`），使命令字符串本身不等于成功证据。
3. 输出搜索范围别只查前 240 行，避免长跑后误失败。
4. 至少一个负向用例：只发字符不按回车，不得判为执行成功。

## R5 · CI 手动触发与各来源的格式检查基准

**证据**：`.github/workflows/ci.yml:33–38` 非 PR 直接用 `github.event.before`；workflow_dispatch 无此值 → `..HEAD` 空范围静默跳过；全零 before SHA → `Invalid revision range`。

**要求**：
- 分别处理 PR（base_ref 三点）、普通 push（before..HEAD）、手动触发、无有效父基准的 push（如全零 before → 用 HEAD~1 或仅 HEAD 提交），每种情况检查哪个变更集合要写明确；手动触发不得无提示等价「零文件通过」（至少 echo 说明）。
- 文件列表用 `git diff --name-only -z` 的 NUL 输出直接喂 `xargs -0`，不要先按换行拆再 `tr` 回 NUL。
- 把基准解析逻辑抽成可测试的小脚本（如 `scripts/ci-changed-range.ts`），四种来源各配测试用例；workflow 里调用它。

## 规则

- 本包涉及真实 tmux：一律隔离 server（沿用 run-e2e 的 TMUX_TMPDIR 方案）+ `test` session，严禁操作用户其他 session。
- eslint / prettier / typecheck 过；相关测试跑通（e2e 若需起服务按现有 run-e2e 流程，注意别占 3001 prod / 3101 dev 端口——用脚本原有的临时端口机制）。
- commit 到 `fix/e2e-smoke-ci`，不 push。
- **不要**重启 prod/dev 实例、不动 dist。
- 完成后回执 `DONE` + 改动文件清单 + 验证结果 + 遗留风险。
