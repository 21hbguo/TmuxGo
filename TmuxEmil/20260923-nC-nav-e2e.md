# N-C：编辑器跳转真实交互 E2E 回归

- worktree：`/home/guo/project/other/TmuxGo-wt-nav-e2e`，分支 `test/nav-e2e`（已建，基于 master 1813fa3）
- 依据：`docs/editor-navigation-boundary-review-20260923-093054.md` §六验收矩阵（先通读全文）
- 目标：**新建** `e2e/editor-navigation.spec.ts`（不碰现有 spec/业务码，零冲突）

## 要求（照文档 §六）

1. 真实浏览器 E2E 用**真实 Monaco**（不替换成 textarea）；fixture 用调用行与定义行明显分离的长文件，函数名列号不取 1（防「永远落 1:1」假过）
2. 覆盖矩阵子集（按可行性优先）：
   - 同文件/跨文件 F12、Ctrl+点击、定义按钮 → 断言模型 URI+声明行列+目标在可见区
   - Back/Forward/Alt+左右交叉 → 源/目标位置与栈一致，快捷键不离开应用
   - 多级 A→B→C 退两次进两次、退回后跳 D 清 forward 分支
   - 慢搜索时 Back → 旧结果不抢回（可在 N-B 修复后转绿）
   - 中转导出跳转 → 真实声明行（可在 N-A 修复后转绿）
3. 读取模型位置用测试专用钩子或可靠观察方式，不暴露多余生产调试接口
4. marker/断言用精确行列，不用 includes 式弱断言
5. **界值/断言可预期红**：N-A/N-B 未合并前相关用例允许挂——你的职责是把红测写实，由我在 A/B 合并后复跑定夺转绿（同 P1-C 模式）

## 安全红线（上轮事故后强制）

- 需要 tmux 时**只用 run-e2e 隔离 server 的 `test` session**；spec 内任何 `execFileSync('tmux')` 必须过 `if (process.env.TMUX || !process.env.TMUX_TMPDIR) throw` 守卫（照抄 render-dedup.spec.ts 的 tmux() 封装）
- 严禁裸 `tmux`、禁 kill-server/kill-session、禁脱离 `scripts/run-e2e.ts` 直跑 spec

## 流程

1. `cd /home/guo/project/other/TmuxGo-wt-nav-e2e && pnpm install`
2. 建 todo；参考 `e2e/render-dedup.spec.ts` 的探针/计数结构、`e2e/terminal-rendering.spec.ts` 的 ensureSession/openSession 基建
3. 基线：`env -u TMUX -u TMUXGO_HOST npx tsx scripts/run-e2e.ts e2e/editor-navigation.spec.ts`（红/绿如实记录）
4. eslint/prettier/typecheck 净；commit（**不 push**），回执 DONE 到 %0：commit、基线红绿、遗留
