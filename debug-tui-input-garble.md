# Debug: tui-input-garble

Status: [OPEN]
runId: pre-fix

## 问题描述
- 实际：桌面浏览器全屏 TUI 程序里，输入一段文字后把光标移回中间插入文字时"乱输入"，之后整个输入持续异常，回到行尾也一样。中英文都会出现。疑似按了 Ctrl 或 Cmd + 方向键后触发。
- 预期：光标移到任意位置插入文字，内容与位置都正确。

## 复现步骤
1. 打开任一 session 的全屏 TUI 程序
2. 输入一段文字
3. 按 Ctrl+方向键 或 Cmd+方向键 移动光标
4. 在中间位置继续输入 → 乱输入

## 假设
| ID | 假设 | 可能性 | 验证信号 |
|----|------|--------|----------|
| A | Cmd/Ctrl+方向键触发浏览器导航或发出程序不认识的序列，程序/屏幕失步 | 中 | onData 异常序列或页面重连事件 |
| B | 前端输入乱序/重复（队列与直发交错） | 低 | 输入日志乱序/重复 |
| C | 输出被 writeBuffer 扣留导致显示与程序真实状态失步 | 高 | corruption 前输出被扣、flush 延迟 |
| D | IME composition 标志卡住 | 中 | compositionstart 无 end |
| E | TUI 程序启用键盘增强协议后修饰键序列被误解 | 中 | 输出含 `\x1b[>4;`/`\x1b[>u` |

## 证据
（待收集）

## 结论
（待定）
